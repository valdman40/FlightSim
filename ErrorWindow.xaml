<UserControl x:Class="FilghtSim.view.ErrorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FilghtSim.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Viewbox>
        <Grid Background="LightPink" Height="400" Width="800">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!--Text="{Binding Path=_ErrorString, Mode=OneWay}"-->
            <TextBox VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="0" Padding="10" Text="{Binding Path=_ErrorString, Mode=OneWay}" FontSize="30"/>
        </Grid>
    </Viewbox>
</UserControl>
